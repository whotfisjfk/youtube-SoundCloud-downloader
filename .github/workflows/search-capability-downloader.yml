name: 🎬🎵 Multi-Platform Downloader (YouTube + SoundCloud + Split to ZIP + Search Integration)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '🌐 پلتفرم'
        required: true
        default: 'youtube'
        type: choice
        options:
          - 'youtube'
          - 'soundcloud'
      url:
        description: '🔗 لینک مستقیم (در صورت عدم استفاده از شماره جستجو)'
        required: false
        default: ''
      search_result_number:
        description: '#️⃣ شماره نتیجه جستجو (مثلاً ۳) - جایگزین لینک'
        required: false
        default: ''
      format:
        description: '🎬 خروجی'
        required: true
        default: 'video'
        type: choice
        options:
          - 'video'
          - 'audio'
      folder:
        description: '📁 پوشهٔ ذخیره'
        required: false
        default: 'downloads'

jobs:
  download:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: 📥 دریافت فایل‌های مخزن (شامل search_results.json)
        uses: actions/checkout@v4

      - name: 🔧 نصب ffmpeg و ابزارهای کمکی
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg zip jq

      - name: ⚙️ نصب yt‑dlp با پشتیبانی از چالش‌های JavaScript
        uses: AnimMouse/setup-yt-dlp@v3
        with:
          ejs-node: true
          aria2: true

      - name: 🧹 پاکسازی پوشهٔ دانلود
        run: rm -rf "${{ github.event.inputs.folder }}"/*

      - name: 🔢 دریافت لینک از شماره جستجو (در صورت ورود)
        id: resolve_url
        run: |
          if [ -n "${{ github.event.inputs.search_result_number }}" ]; then
            INDEX="${{ github.event.inputs.search_result_number }}"
            if [ ! -f search_results.json ]; then
              echo "❌ فایل search_results.json یافت نشد. ابتدا یک جستجو انجام دهید."
              exit 1
            fi
            URL=$(jq -r ".[] | select(.index == $INDEX) | .url" search_results.json)
            if [ -z "$URL" ]; then
              echo "❌ شماره $INDEX در نتایج جستجو وجود ندارد."
              exit 1
            fi
            echo "✅ لینک بازیابی شده از جستجو: $URL"
            echo "RESOLVED_URL=$URL" >> $GITHUB_OUTPUT
          else
            echo "RESOLVED_URL=${{ github.event.inputs.url }}" >> $GITHUB_OUTPUT
          fi

      # ================= YouTube =================
      - name: ▶️ یوتیوب | دانلود ویدئو (MP4)
        if: github.event.inputs.platform == 'youtube' && github.event.inputs.format == 'video'
        run: |
          mkdir -p "${{ github.event.inputs.folder }}"
          yt-dlp -f "bestvideo[height<=1080]+bestaudio/best[height<=1080]" \
            --merge-output-format mp4 \
            --cookies cookies.txt \
            --force-overwrites \
            "${{ steps.resolve_url.outputs.RESOLVED_URL }}" \
            -o "${{ github.event.inputs.folder }}/%(title)s.%(ext)s"

      - name: ▶️ یوتیوب | دانلود صوت (MP3)
        if: github.event.inputs.platform == 'youtube' && github.event.inputs.format == 'audio'
        run: |
          mkdir -p "${{ github.event.inputs.folder }}"
          yt-dlp -f "bestaudio[ext=m4a]/bestaudio/best" \
            --audio-format mp3 \
            --audio-quality 0 \
            --cookies cookies.txt \
            --force-overwrites \
            --no-playlist \
            "${{ steps.resolve_url.outputs.RESOLVED_URL }}" \
            -o "${{ github.event.inputs.folder }}/%(title)s.%(ext)s"

      # ================= SoundCloud =================
      - name: ☁️ ساندکلاد | دانلود صوت (MP3)
        if: github.event.inputs.platform == 'soundcloud' && github.event.inputs.format == 'audio'
        run: |
          mkdir -p "${{ github.event.inputs.folder }}"
          yt-dlp -x --audio-format mp3 --audio-quality 0 \
            --no-playlist \
            --force-ipv4 \
            "${{ steps.resolve_url.outputs.RESOLVED_URL }}" \
            -o "${{ github.event.inputs.folder }}/%(title)s.%(ext)s"

      - name: ☁️ ساندکلاد | دانلود ویدئو (در صورت وجود)
        if: github.event.inputs.platform == 'soundcloud' && github.event.inputs.format == 'video'
        run: |
          mkdir -p "${{ github.event.inputs.folder }}"
          yt-dlp -f bestvideo+bestaudio/best --merge-output-format mp4 \
            --no-playlist \
            "${{ steps.resolve_url.outputs.RESOLVED_URL }}" \
            -o "${{ github.event.inputs.folder }}/%(title)s.%(ext)s"

      # ================= بسته‌بندی ویدئوهای بزرگ به ZIP چندبخشی =================
      - name: ✂️ تبدیل ویدئوهای بزرگ به ZIP چندبخشی
        run: |
          FOLDER="${{ github.event.inputs.folder }}"
          for video in "$FOLDER"/*.mp4; do
            [ -e "$video" ] || continue
            SIZE=$(stat -c%s "$video")
            if [ "$SIZE" -gt 94371840 ]; then
              BASENAME=$(basename "$video" .mp4)
              ZIPNAME="$FOLDER/${BASENAME}.zip"
              echo "بسته‌بندی $video در $ZIPNAME ..."
              zip -s 90m -j "$ZIPNAME" "$video"
              rm "$video"
              echo "✅ فایل به قطعات ZIP تقسیم شد. برای بازیابی، همهٔ قطعات را دانلود کرده و فایل .zip را با WinRAR باز کنید." > "$FOLDER/${BASENAME}_README.txt"
            fi
          done

      # ================= ذخیره در مخزن =================
      - name: 🚀 ثبت فایل‌ها در مخزن
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "${{ github.event.inputs.folder }}"
          git commit -m "افزودن فایل از ${{ github.event.inputs.platform }}" || echo "هیچ فایل جدیدی نیست"
          git push

      # ================= انتشار در Release (فقط برای فایل‌های یکپارچه) =================
      - name: 📦 انتشار در Release (فایل‌های کوچک)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          FILE=$(find "${{ github.event.inputs.folder }}" -maxdepth 1 -type f \( -name "*.mp3" -o -name "*.mp4" \) 2>/dev/null | head -1)
          if [ -z "$FILE" ]; then
            echo "فایل شکسته شده است؛ از Release صرف‌نظر می‌شود. قطعات را از پوشه دانلود کنید."
            exit 0
          fi
          echo "آپلود فایل یکپارچه: $FILE"
          if gh release view latest &> /dev/null; then
            gh release upload latest "$FILE" --clobber
          else
            gh release create latest "$FILE" \
              --title "مجموعه دانلودهای چندمنظوره" \
              --notes "فایل‌های یکپارچه (زیر 90MB) اینجا قرار می‌گیرند."
          fi
          echo "📎 لینک Release: https://github.com/${{ github.repository }}/releases/tag/latest"
